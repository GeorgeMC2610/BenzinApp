import 'package:flutter/material.dart';

class PricePerLiter extends StatelessWidget {
  const PricePerLiter({super.key});

  @override
  Widget build(BuildContext context) => const Column(
    children: [
      Text("Booyah from PRICE PER LITER!")
    ],
  );

}