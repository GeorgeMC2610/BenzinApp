import 'package:flutter/material.dart';

class FfrStats extends StatelessWidget {
  const FfrStats({super.key});

  @override
  Widget build(BuildContext context) => const Column(
    children: [
      Text("Booyah from ffr statssss!")
    ],
  );

}